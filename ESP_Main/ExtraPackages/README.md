# Purpose

The purpose of this folder is to house all of the extra packages needed to make
the robot compile. Due to the gitsubmodules, it is not possible to have these
directly included in the commits. Therefore, all of the packages have copies
here, and then when downloading the repo, copy all of these folders into the
`micro_ros_espidf_component/extra_packages/` folder.
